import SwiftUI

/// More tab — hosts the Settings content (Audio/Haptics, How to Play, Privacy Policy,
/// About/Version, Reset Progress).
struct MoreView: View {
    var body: some View {
        SettingsView()
    }
}
